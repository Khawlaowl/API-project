const express = require('express')
const app = express()
const PORT = 1337

app.listen(PORT,() => console.log(`server has started on port :${PORT} `))


const express = require('express');
const app = express();
const PORT = 0000; // Replace with your desired port number

app.listen(PORT, () => {
    console.log(`Server has started on port: ${PORT}`);
});

